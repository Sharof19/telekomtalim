import 'package:flutter/material.dart';

class HomePalette {
  // Bitta brend rangi — barcha joyda shundan foydalanamiz
  static const Color brandBlue = Color.fromRGBO(57, 100, 149, 1);

  static const Color background = Color(0xFFE8ECF5);
  static const Color primary = brandBlue;
  static const Color accent = brandBlue;
  static const Color card = Colors.white;
  static const Color border = Color(0xFFE1E5F2);
  static const Color textMuted = Color(0xFF6B7280);
}
